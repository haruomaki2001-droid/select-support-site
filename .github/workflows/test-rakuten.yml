name: Test Rakuten API

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Verify productCode and rank together
        env:
          RAKUTEN_APPLICATION_ID: ${{ secrets.RAKUTEN_APPLICATION_ID }}
          RAKUTEN_ACCESS_KEY: ${{ secrets.RAKUTEN_ACCESS_KEY }}
        run: |
          curl -s -G "https://openapi.rakuten.co.jp/ichibaproduct/api/Product/Search/20250801" \
            -H "accessKey: ${RAKUTEN_ACCESS_KEY}" \
            -H "Origin: https://supisho.com" \
            -H "Referer: https://supisho.com" \
            --data-urlencode "applicationId=${RAKUTEN_APPLICATION_ID}" \
            --data-urlencode "format=json" \
            --data-urlencode "genreId=560202" \
            --data-urlencode "hits=10" \
            --data-urlencode "elements=productName,productCode,rank,makerName,salesMinPrice"